{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:acp:schemas:companion:canonicalization_binding_v1",
  "title": "canonicalization_binding_v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "binding_type",
    "binding_version",
    "canonicalization_id",
    "algorithm",
    "created_at"
  ],
  "properties": {
    "binding_type": {
      "const": "canonicalization_binding_v1"
    },
    "binding_version": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$"
    },
    "canonicalization_id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._:/-]+$"
    },
    "algorithm": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._-]+$"
    },
    "created_at": {
      "type": "string",
      "minLength": 1,
      "format": "date-time"
    }
  }
}
