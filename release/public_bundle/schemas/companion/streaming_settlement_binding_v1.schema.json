{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:acp:schemas:companion:streaming_settlement_binding_v1",
  "title": "streaming_settlement_binding_v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "binding_type",
    "binding_version",
    "channel_type",
    "idempotency_mode",
    "created_at"
  ],
  "properties": {
    "binding_type": {
      "const": "streaming_settlement_binding_v1"
    },
    "binding_version": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$"
    },
    "channel_type": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._-]+$"
    },
    "idempotency_mode": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._-]+$"
    },
    "created_at": {
      "type": "string",
      "minLength": 1,
      "format": "date-time"
    }
  }
}
